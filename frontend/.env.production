# Empty on purpose: requests go to relative /api/*, which Vercel's rewrite proxy
# (vercel.json) forwards to the EC2 backend server-side. Keeps the browser on HTTPS,
# so no mixed-content block and no HTTPS setup needed on the backend.
VITE_API_URL=
