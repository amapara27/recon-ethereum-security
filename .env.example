# This is an example environment file for reference
# The actual configuration is handled through vercel.json

# In production (Vercel), API calls to /api/* are proxied to the AWS backend
# In development, vite.config.js proxies /api/* to http://localhost:8000
