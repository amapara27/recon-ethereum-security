pandas>=1.5.0
numpy>=1.23.0
# Pinned to the version fraud_model.joblib was trained with — a mismatched
# unpickle is unsupported by sklearn and can silently produce invalid scores.
scikit-learn==1.7.2
joblib>=1.2.0
web3>=6.0.0
python-dotenv>=1.0.0
requests>=2.28.0
jupyter>=1.0.0
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.0.0
anthropic
# Optional / dev (not strictly required for runtime but useful for notebooks)
ipykernel>=6.0.0
